# =============================================================================
# requirements_cpu_extras.txt — pachete pentru masini FARA GPU NVIDIA
# (ex: TORINO-ASPIRE5 cu doar Intel iGPU). Instalate de ACTUALIZARI.bat DOAR
# daca .machine_profile zice GPU_TYPE=CPU_ONLY.
#
# Stack-ul AI greu (timesfm, chronos, momentfm, neuralforecast, transformers)
# E DELIBERAT EXCLUS — pe CPU rulează 30-60s per inference, iar engine-ul are
# fallback determinist (_fallback_scores_no_tfm) care merge fără ele.
#
# Daca vrei totusi sa testezi foundation models pe CPU (slow), instaleaza
# manual:  pip install timesfm chronos-forecasting momentfm
#
# TORCH-CPU wheel se instaleaza separat din index PyTorch:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# =============================================================================

# (Lista goala intentionat — pe CPU mode nu instalam nimic AI greu.
# Tot ce trebuie e in requirements_base.txt + torch+cpu instalat separat.)
