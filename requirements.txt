# Compatibilitate pentru `pip install -r requirements.txt`.
# Sursa UNICĂ este requirements_base.txt (CPU-only). Folosirea directă a
# fișierului vechi reinstala din greșeală streamlit + numba, eliminate din cod.
-r requirements_base.txt
